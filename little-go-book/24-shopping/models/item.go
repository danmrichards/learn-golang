package models

// Item - A shopping item.
type Item struct {
    // Fields starting with an Uppercase letter are visible. If it was price (
    // with a lowercase p) it would not be visible.
    Price float64
}
